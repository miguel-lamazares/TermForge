[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "termforge"
version = "0.1.0"
description = "Personal terminal utilities"
authors = [{ name = "Dex" }]
